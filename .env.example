# Copy this file to .env if you need any of the values below.
# .env is git-ignored — never commit real keys.
#
# Nothing here is required to run the site.
#
# Live crypto rates come straight from CoinGecko's public API
# (https://docs.coingecko.com/) — no API key, no backend, no proxy.
# See src/lib/cryptoRates.ts.
